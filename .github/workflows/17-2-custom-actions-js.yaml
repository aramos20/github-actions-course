name: 17 - 2 - Custom Actions - JS

run-name: 17 - 2 - Custom Actions - JS

on:
  workflow_dispatch:

jobs:
  dependecy-update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Check for dependencies updates
        uses: ./.github/actions/js-dependecy-update
